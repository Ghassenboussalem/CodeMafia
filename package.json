{
  "name": "codemafia",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time multiplayer social deduction coding game",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "format": "prettier --write \"**/*.{js,jsx,json,css,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  }
}
